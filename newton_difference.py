print("좌표의 개수를 입력하세요>>")
n=int(input())
x_values=[]
y_values=[]
lst=[]
for i in range(n):
    print("좌표를 입력해주세요")
    a,b=map(int,input().split())
    x_values.append(a)
    y_values.append(b)
def difference_interpolation(x,y,n):
    i=0
    while(len(lst)<2):
        for k in range(len(lst)-1):
            k in range
        i+=1